#include <stdio.h>

int main()
{
    int a,b,i,c=o;
    scanf("%d %d",&a,&b);
    for(i=1;;i++)
    {
        if(i%2==0&&i%a==0)
        {
            printf("%d",i);
            c++;
            if(c==b)
            break
        }
    }
return 0;
}
